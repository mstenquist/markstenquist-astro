---
const today = new Date();
---

<footer>
	<span class="copyright">&copy; {today.getFullYear()} mark_stenquist</span>
	<span class="separator">/</span>
	<span class="tagline">building for the web since 2009</span>
</footer>
<style>
	footer {
		padding: 2rem;
		background: var(--surface, #fdfcfa);
		color: var(--text-muted, #b8a589);
		text-align: center;
		font-family: var(--font-mono, 'JetBrains Mono', monospace);
		font-size: 0.75rem;
		letter-spacing: 0.02em;
		border-top: 1px solid var(--border, #e6ddd0);
		margin-top: 2rem;
	}
	
	.copyright {
		color: var(--text-secondary, #7a6a52);
	}
	
	.separator {
		margin: 0 0.75rem;
		color: var(--border, #e6ddd0);
	}
	
	.tagline {
		color: var(--text-muted, #b8a589);
	}

	@media (max-width: 480px) {
		footer {
			padding: 1.5rem 1rem;
			display: flex;
			flex-direction: column;
			gap: 0.25rem;
		}
		
		.separator {
			display: none;
		}
	}
</style>
